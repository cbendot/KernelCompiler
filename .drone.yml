kind: pipeline
name : xKernelCompiler
version: 2.1
steps:
 - name: xKernelCompiler Compilation
   image: z4nyx/docker:latest
   environment:
     chat_id:
       from_secret: CHAT_ID
     token:
       from_secret: TOKEN
   commands:
       - bash build.sh
